{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }} | Créer une sortie
{% endblock %}

{% block body %}

    <body>


    <h1>Create Sortie</h1>

    {# Ouvrir le formulaire #}
    {{ form_start(sortieForm) }}

    {# Afficher tout les champs #}
    {{ form_widget(sortieForm) }}

    <div class="uk-margin">
        <button type="submit" class="uk-button uk-button-primary uk-width-1-1 uk-margin-small-bottom">Save</button>
    </div>

    {# Fermer le formulaire #}
    {{ form_end(sortieForm) }}
    </body>
{% endblock %}

